<div id="<%= dom_id post %>">
  <p>
    <strong>Tittle:</strong>
    <%= post.tittle %>
    <p> <%= pluralize(post.views, "view") %> </p>
    <p> publicado por: <%= post.user.email %> </p>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
